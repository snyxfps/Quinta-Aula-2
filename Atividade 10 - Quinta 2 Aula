# lê o número de maçãs compradas
num_macas = int(input("Digite o número de maçãs compradas: "))

# verifica o preço unitário das maçãs
if num_macas < 12:
  preco_unitario = 0.30
else:
  preco_unitario = 0.25

# calcula o valor total da compra
valor_total = num_macas * preco_unitario

# exibe o valor total da compra
print("O valor total da compra é R$ {:.2f}".format(valor_total))
